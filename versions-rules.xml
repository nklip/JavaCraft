<?xml version="1.0" encoding="UTF-8"?>
<ruleset
        comparisonMethod="maven"
        xmlns="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0 https://www.mojohaus.org/versions/versions-model/xsd/rule-2.0.0.xsd">

    <ignoreVersions>
        <ignoreVersion type="regex">(?i).*-alpha[0-9A-Za-z.-]*</ignoreVersion>
        <ignoreVersion type="regex">(?i).*-beta[0-9A-Za-z.-]*</ignoreVersion>
        <ignoreVersion type="regex">(?i).*-rc[0-9A-Za-z.-]*</ignoreVersion>
        <ignoreVersion type="regex">(?i).*-milestone[0-9A-Za-z.-]*</ignoreVersion>
        <ignoreVersion type="regex">(?i).*-M[0-9]*</ignoreVersion>
        <ignoreVersion type="regex">(?i).*-m[0-9]+[0-9A-Za-z.-]*</ignoreVersion>
    </ignoreVersions>
</ruleset>
